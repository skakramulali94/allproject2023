git init
git add -A
git commit -m "all commit"
git commit -m "2ndcommit"

git branch -M master

git remote add origin https://github.com/skakramulali94/allproject.git

git push -u origin master

if cannot push:-
git push -f origin master
